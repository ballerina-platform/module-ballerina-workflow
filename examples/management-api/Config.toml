# Workflow engine — runs against a local Temporal server for development and tests.
[ballerina.workflow]
mode = "LOCAL"

# Management API — exposed at http://localhost:7234/workflow/
[ballerina.workflow.management]
enableManagementApi = true
port              = 7234
enableBasicAuth   = false
